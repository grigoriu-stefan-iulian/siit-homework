import React from 'react'

const Header = () => (
    <div>
        <h1>Weather App in React</h1>
        <p>The user can have a preference whether to see the weather in C or in F.</p>
        <p>Store this in the local storage and use it to display the temperature for the user based on the stored value.</p>
        <p> Use cookies if local storage is not supported in the browser</p>
    </div>
)

export default Header