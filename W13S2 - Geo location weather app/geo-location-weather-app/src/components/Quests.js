import React from 'react'

const Quests = () => (
    <div className="quests">
        <p>Combine Geolocation API and Fetch API to get the weather for your current location:</p>
        <p>Use navigator.geolocation.getCurrentPosition to get the current position</p>
        <p>Use that when making the call to the Weather API (ex: https://api.wunderground.com/api/....)</p>
    </div>
)

export default Quests